[project]
name = "cn-invoice-parser"
version = "0.1.0"
description = "A tool to parse Chinese electronic invoices"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pdfplumber>=0.11.0",
    "openai>=1.0.0",
]

[tool.uv]
dev-dependencies = []
